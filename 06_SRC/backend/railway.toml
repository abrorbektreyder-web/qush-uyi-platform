[build]
builder = "nixpacks"
nixpacksPlan = { phases = { setup = { aptPkgs = ["ffmpeg"] } } }

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/docs"
healthcheckTimeout = 100
